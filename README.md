# Crypto Bot 📈

Простой и эффективный Telegram-бот на языке Go для мониторинга курсов криптовалют в реальном времени с использованием API Binance.

## Основные возможности

- **Проверка курсов**: Получение актуальной цены любой монеты к USDT.
- **Автоматические подписки**: Возможность подписаться на уведомления о цене конкретной монеты с заданным интервалом.
- **Персистентность**: Сохранение всех подписок в файл `subs.json`. При перезагрузке бот автоматически восстанавливает все активные таймеры мониторинга.
- **Конкурентность**: Использование Go-рутин для независимого отслеживания цен для каждого пользователя.

## Команды бота

| Команда                     | Описание                                     | Пример              |
| :-------------------------- | :------------------------------------------- | :------------------ |
| `/price`                    | Показывает текущий курс BTC и ETH            | `/price`            |
| `/coinprice [symbol]`       | Показывает курс конкретной монеты            | `/coinprice sol`    |
| `/subscribe [symbol] [min]` | Подписка на уведомления (в минутах)          | `/subscribe btc 10` |
| `/unsubscribe`              | Удаление всех активных подписок пользователя | `/unsubscribe`      |

## Технологический стек

- **Язык**: Go (Golang)
- **Библиотека бота**: `gopkg.in/telebot.v3`
- **Источник данных**: Binance Public REST API
- **Хранение данных**: JSON файл

## Установка и запуск

1.  **Клонируйте репозиторий**:

    ```bash
    git clone https://github.com/lostofreference/crypto-bot-on-go.git
    cd crypto-bot-on-go
    ```

2.  **Установите зависимости**:

    ```bash
    go mod tidy
    ```

3.  **Настройте токен**:
    Замените значение переменной `Token` в файле `main.go` на токен вашего бота, полученный от [@BotFather](https://t.me/BotFather).

4.  **Запустите бота**:
    ```bash
    go run main.go
    ```

## Как это работает (под капотом)

Бот использует `http.Get` для запроса данных с Binance. При создании подписки запускается отдельная горутина с `time.NewTicker`. Безопасность данных при одновременном доступе из разных горутин обеспечивается с помощью `sync.Mutex`.

---
